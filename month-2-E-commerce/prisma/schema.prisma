datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

// 👤 Temporary model to demonstrate Prisma generation benefits
model User {
  id    Int     @id @default(autoincrement())
  email String  @unique
  name  String?
}
